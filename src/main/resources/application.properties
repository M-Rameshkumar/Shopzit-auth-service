# Application Name
spring.application.name=Shopzit

# Server Configuration
server.port=9080

# Database Configuration (Use Docker Service Name as Host)
spring.datasource.url=jdbc:postgresql://Shopzit-DB:5432/AuthShopzit
#spring.datasource.url=jdbc:postgresql://localhost:5432/AuthShopzit


spring.datasource.username=postgres
spring.datasource.password=ramesh123
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA Configuration
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=update

spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

logging.level.org.hibernate.SQL=DEBUG
logging.level.org.hibernate.type.descriptor.sql.BasicBinder=TRACE

# CORS and Client URL Configuration
app.client.url=http://Shopzit-FE:5173

# Logging
logging.level.org.springframework.security=DEBUG
